{
  "name": "scriptflow-pro",
  "version": "2.0.0",
  "description": "ScriptFlow Pro - Smart CRM with Handoff Integration",
  "main": "index.html",
  "scripts": {
    "start": "npx serve . -p $PORT --cors",
    "dev": "npx serve . -p 3000 --cors",
    "build": "echo 'Static site - no build needed'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}