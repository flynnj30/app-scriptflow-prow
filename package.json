{
  "name": "scriptflow-pro",
  "version": "2.2.0",
  "private": true,
  "description": "ScriptFlow Pro - Smart CRM with an in-app Transcript Studio browser surface",
  "main": "dist/server.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "node dist/server.js",
    "dev": "npm run build && node dist/server.js"
  },
  "engines": {
    "node": ">=20.0.0 <25.0.0"
  },
  "dependencies": {
    "esbuild": "^0.25.9"
  }
}
